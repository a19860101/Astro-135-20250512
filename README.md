# Git 版本控制

## 指令

```bash
#初始化
git init

# 設定姓名email
git config --global user.name Your Name
git config --global user.email Your email

# 查詢設定檔
git config --list


# 加入暫存
git add .

# 查看狀態
git status

# 提交
git commit -m "訊息"

# 查看版本紀錄
git log
git log --oneline

```